#include "Track.h"

Track::Track(Actor* ownerP, int updateOrderP)
    : Actor(ownerP, updateOrderP) 
{
    // Set up the track's properties and initialize
}

void Track::update(float dt)
{
    // Update track's position, check for collisions, etc.
}