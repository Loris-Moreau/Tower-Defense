#include "Car.h"

Car::Car(Actor* ownerP, int updateOrderP)
{
    // Set up the car's sprite and other initialization
}

void Car::processInput(const Uint8* keyState)
{
    // Handle user input for steering and acceleration
}

void Car::update(float dt)
{
    // Update car's position, check for collisions, etc.
}